@page "/player-formula-scrambler"
@layout RegisterLayout
@namespace HaveFun.Web

<PageTitle>Player Formula Scrambler</PageTitle>

<MudStack Spacing="4" Class="pa-4">
    @if (!IsSessionChecked)
    {
        <MudAlert Severity="Severity.Info" Variant="Variant.Outlined" Dense="true">
            Checking player session
        </MudAlert>
    }
    else if (ErrorMessage is not null)
    {
        <MudAlert Severity="Severity.Error" Variant="Variant.Outlined">
            @ErrorMessage
        </MudAlert>
        <MudButton Href="/register"
                   Color="Color.Primary"
                   Variant="Variant.Filled"
                   StartIcon="@Icons.Material.Filled.Login">
            Register
        </MudButton>
    }
    else if (CurrentRound is not null)
    {
        <PlayerGameBoard Title="Formula Scrambler Game"
                         Rules="Rule: arrange the available characters into a mathematically correct equation."
                         RoundId="@CurrentRound?.Id"
                         AvailableTiles="@(PlayerRoundState?.AvailableTiles ?? [])"
                         SelectedTiles="@(PlayerRoundState?.SelectedTiles ?? [])"
                         IsSubmitted="@(PlayerRoundState?.IsSubmitted == true)"
                         TileTextSeparator=""
                         SpentTime="@PlayerRoundState?.SpentTime"
                         RemainingTimeText="@RemainingTimeText"
                         CanSubmit="@CanSubmit"
                         OnSubmit="SubmitRound" />
    }
</MudStack>
